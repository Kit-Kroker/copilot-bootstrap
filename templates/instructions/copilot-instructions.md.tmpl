You are working on **{{PROJECT_NAME}}**, a {{ARCH_STYLE}} project.

## Tech Stack

- **Language**: {{LANGUAGE}}
- **Backend**: {{BACKEND}}
- **Frontend**: {{FRONTEND}}
- **Database**: {{DB}}
- **Package manager**: {{PKG_MANAGER}}

## Development Workflow

- Run tests: `{{TEST_RUNNER}}`
- Lint: `{{LINTER}}`
- Build: `{{BUNDLER}}`

## Project Structure

- Source: `{{SRC_PATH}}`
- Tests: `{{TESTS_PATH}}`

## Conventions

- Follow {{LANGUAGE}} best practices and idioms
- Keep business logic in `{{SRC_PATH}}`
- Mirror test structure to source structure in `{{TESTS_PATH}}`
- Never commit secrets or credentials
- Run `{{LINTER}}` before submitting changes
- Ensure `{{TEST_RUNNER}}` passes before opening a PR
